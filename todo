evryething the client writes to be put in a file within a folder somewhere that would be cool 

i want to re implement the type "\q" to end the clients connection to server 

maybe show the server in a list labed with an id and making client have to accept connection